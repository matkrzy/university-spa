import { socket } from '../socket';

import { MARKET_GET, MARKET_UPDATE } from './action-types';

//socket.on(UPDATE_MARKET, data => {
//  console.log(data);
//  this.props.updateGlobalMarket(data);
//});
//
//
//socket.on('update-model', model => {
//  this.setState({ model });
//});
