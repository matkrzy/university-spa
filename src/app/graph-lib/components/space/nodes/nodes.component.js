import { Component } from 'react';

/** Class representing a `NodesComponent`. Wrapper for nodes
 * @extends Component
 */
export class NodesComponent extends Component {
  render() {
    return this.props.children;
  }
}
